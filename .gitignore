# Secrets
.env
.env.*
!.env.example

# Environment
.venv/
__pycache__/
*.py[cod]

# Data and run artefacts. Anchored to the repo root: an unanchored "data/"
# also matches src/handoff/data/, which silently excluded the whole data
# package from the repository.
/data/
/runs/

# Per-user Claude Code settings (shared settings live in .claude/settings.json)
.claude/settings.local.json

# OS / editor
.DS_Store
.idea/
.vscode/
